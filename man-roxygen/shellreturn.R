#' @return 0 for successful execution unless \code{intern=TRUE} is part of \code{...} and if it is,
#'         output from the shell will be returned rather than printed to the
#'         screen for most commands.

